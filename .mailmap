# Re-attribute Cursor co-author trailers still reachable via refs/pull/*/head
# (GitHub keeps those permanently). Display-time only; does not rewrite history.
Michael Bernstein <michael@hookdeploy.dev>
